from typing import List
import numpy as np

def load_calibration_data(dataset_path: str, num_samples: int) -> List[np.ndarray]:
    """
    Loads calibration images from a dataset for static quantization.

    :param dataset_path: Path to the dataset containing images.
    :param num_samples: Number of images to load.
    :return: A list of images as NumPy arrays.
    """
    pass
